{
  "name": "glamedge-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    
      "start-servers": "concurrently \"json-server --watch category.json --port 3001\" \"json-server --watch subcategory.json --port 3002\" \"json-server --watch products.json --port 3003\""
    
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
},
  "devDependencies": {
    "concurrently": "^8.2.1"
  }
}
